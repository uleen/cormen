import React from 'react';
import { HeaderWrapper } from './styles';
import { Cart } from 'components';

export const Header = () => {
  return (
    <HeaderWrapper>
      <Cart />
    </HeaderWrapper>
  );
};
